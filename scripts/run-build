#!/bin/sh -eux
echo run-build $1
if docker run -v gitroot:/gitroot --shm-size=1024M --name $S5CI_JOB_NAME aytest /start.sh $1 $2 ; then
	echo Docker Success
else
	DOCKER_RES=$?
	echo Docker run result: ${DOCKER_RES}
fi
# | ts
sleep 5
exit 123
