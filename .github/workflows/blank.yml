name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
#    - name: install rust
#      run: make install-rust
#    - name: Make rust
#      run: make build
    - name: build a golang version
      run: |
        sudo apt-get install golang
        (cd go-s5ci; go get || true; go build; ls -al)
